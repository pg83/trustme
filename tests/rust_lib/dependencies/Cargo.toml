[package]
name = "rust-lib-test-dependencies"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
rand = { version = "0.9.0", default-features = false, features = ["alloc"] }
rand_xorshift = { version = "0.4.0", default-features = false }
