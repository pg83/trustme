// Extracted from library/core/src/num/nonzero.rs:1011
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {




assert_eq!(four, two.saturating_mul(two));
assert_eq!(max, four.saturating_mul(max));
Some(())
}
