// Extracted from library/core/src/num/nonzero.rs:972
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {




assert_eq!(Some(four), two.checked_mul(two));
assert_eq!(None, max.checked_mul(two));
Some(())
}
