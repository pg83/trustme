// Extracted from library/core/src/num/int_macros.rs:3561
#![allow(unused)]
fn main() {
    assert_eq!(
        bytes,
        if cfg!(target_endian = "big") {
    
        } else {
    
        }
    );
}
