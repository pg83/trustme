// Extracted from library/core/src/num/nonzero.rs:1625
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {

assert!(eight.is_power_of_two());

assert!(!ten.is_power_of_two());
Some(())
}
