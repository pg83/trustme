// Extracted from library/core/src/num/nonzero.rs:1752
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {



assert_eq!(pos, pos.abs());
assert_eq!(pos, neg.abs());
Some(())
}
