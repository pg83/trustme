// Extracted from library/core/src/num/nonzero.rs:1782
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {




assert_eq!(Some(pos), neg.checked_abs());
assert_eq!(None, min.checked_abs());
Some(())
}
