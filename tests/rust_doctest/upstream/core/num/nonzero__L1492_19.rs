// Extracted from library/core/src/num/nonzero.rs:1492
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {





assert_eq!(Some(two), two.checked_next_power_of_two() );
assert_eq!(Some(four), three.checked_next_power_of_two() );
assert_eq!(None, max.checked_next_power_of_two() );
Some(())
}
