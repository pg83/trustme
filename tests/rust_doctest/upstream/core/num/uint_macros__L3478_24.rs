// Extracted from library/core/src/num/uint_macros.rs:3478
#![allow(unused)]
fn main() {
    assert_eq!(
        bytes,
        if cfg!(target_endian = "big") {
    
        } else {
    
        }
    );
}
