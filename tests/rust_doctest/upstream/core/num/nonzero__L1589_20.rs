// Extracted from library/core/src/num/nonzero.rs:1589
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {




assert_eq!(one.midpoint(four), two);
assert_eq!(four.midpoint(one), two);
Some(())
}
