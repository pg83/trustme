// Extracted from library/core/src/num/nonzero.rs:1423
use std::num::NonZero;

fn main() { test().unwrap(); }
fn test() -> Option<()> {




assert_eq!(two, one.saturating_add(1));
assert_eq!(max, max.saturating_add(1));
Some(())
}
