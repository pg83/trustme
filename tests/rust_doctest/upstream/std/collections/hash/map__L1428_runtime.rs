// Extracted from library/std/src/collections/hash/map.rs:1428
#![allow(unused)]
fn main() {
    use std::collections::HashMap;
    
    let map = HashMap::from([
        ("a", 1),
    ]);
    let iter = map.iter();
}
