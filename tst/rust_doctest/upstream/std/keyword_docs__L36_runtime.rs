// Extracted from library/std/src/keyword_docs.rs:36
#![allow(unused)]
fn main() {
    #[allow(unused_imports)]
    use std::{mem as memory, net as network};
    // Now you can use the names `memory` and `network` to refer to `std::mem` and `std::net`.
}
